# Generated by px-plugin init
plugin: com.powerx.plugins.scrm
channels:
  - stable
  - beta
rollout:
  strategy: canary
  batches:
    - percentage: 20
      wait: 10m
    - percentage: 80
      wait: 20m
rollback:
  strategy: automatic
  maxFailingTenants: 5
