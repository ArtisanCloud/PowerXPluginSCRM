# Dev Console Components

Reusable Vue components for the admin console live here—card presenters, tables, dialogs, and help
panels. Follow the Nuxt UI 3.3 guidelines (two-space indent, `UModal`, `USwitch`, etc.) and keep
components presentational, delegating data access to stores or composables.
