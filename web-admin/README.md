# Powerx Plugin Scrm Plugin Web Admin

该目录基于 Nuxt 4 与 `@artisan-cloud/plugin-framework-admin` Layer，默认启用 Starter 页面。

```bash
npm install
npm run dev
```

若需要覆盖框架默认页面，可在 `app/pages` 或 `app/components` 下新增同路径文件。
